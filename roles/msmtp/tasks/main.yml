---
- name: "Install msmtp"
  become: True
  apt:
    name: "msmtp"

- name: "Symlink the msmtp configuration"
  file:
    src: "{{ roles }}/msmtp/files/msmtprc"
    dest: "{{ home }}/.msmtprc"
    state: "link"
    force: True
    owner: "{{ ansible_user }}"
